node_modules/
dist/
.DS_Store
*.log
.env
.env.local

# Contains DB migration SQL that has historically included live secrets
# (JWT signing secret, bootstrap passwords) pasted directly into function
# bodies. Never committed from here on — run these locally against
# Supabase only, never push them.
supabase/
